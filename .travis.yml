dist: trusty
sudo: required

language: cpp

compiler:
  - clang

branches:
  only:
    - master

install:
  - bash build_support/install_gbench.sh

before_script:
  - mkdir build && cd build
  - cmake -DBENCHMARK_INC_DIR=/usr/local/include -DBENCHMARK_LIB_DIR=/usr/local/lib ..

script:
  - make